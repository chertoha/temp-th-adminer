import React from "react";
import { GlobalStyles } from "@mui/material";
// import { theme } from "./theme";

export default (
  <GlobalStyles
    styles={{
      // h1: { color: "red" },
      body: {},

      ul: {
        margin: 0,
        padding: 0,
        listStyleType: "none",
      },

      button: {
        cursor: "pointer",
        padding: 0,
        margin: 0,
      },
    }}
  />
);
