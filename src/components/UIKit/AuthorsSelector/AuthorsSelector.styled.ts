import { SxProps } from "@mui/material";

const autocomplete: SxProps = { flexGrow: 1, backgroundColor: "white" };

const item: SxProps = { display: "flex", alignItems: "center", columnGap: 6 };

const avatar: SxProps = { width: "64px", height: "64px" };

const styles = { item, avatar, autocomplete };
export default styles;
