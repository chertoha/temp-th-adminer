import { Typography } from "@mui/material";

const NotSupported = () => {
  return (
    <Typography variant="h5" textAlign="center" width="70%" mx="auto" mt="40vh">
      Нажаль панель адміністратора не підтримується на маленьких екранах!
    </Typography>
  );
};

export default NotSupported;
