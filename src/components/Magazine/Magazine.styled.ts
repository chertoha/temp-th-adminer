import { SxProps } from "@mui/material";

const section: SxProps = { backgroundColor: "color.piggy100", mt: "4px" };

const commentWrapper: SxProps = {
  mt: "32px",
  padding: "40px",
  backgroundColor: "#EDEDED",
  borderRadius: "10px",
};

const styles = { commentWrapper, section };
export default styles;
