import { SxProps } from "@mui/material";

const wrapper: SxProps = {
  textAlign: "center",
  pt: "200px",
  color: "color.grey",
};

const heading: SxProps = { fontWeight: "bold" };

const button: SxProps = { mt: "40px", px: "30px", py: "10px", fontSize: 18 };

const styles = { wrapper, heading, button };
export default styles;
