import { SxProps } from "@mui/material";

const input: SxProps = {
  display: "flex",
  alignItems: "center",
  gap: "16px",
};

const submit: SxProps = { mt: "80px", width: "240px", fontSize: "20px" };

const styles = { input, submit };
export default styles;
