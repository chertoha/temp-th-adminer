import { SxProps } from "@mui/material";

const toolbar: SxProps = {
  flexDirection: "row",
  gap: "12px",
  justifyContent: "center",
  color: "text.secondary",
};

const icon: SxProps = { width: "48px", height: "48px" };

const styles = {
  icon,
  toolbar,
};
export default styles;
